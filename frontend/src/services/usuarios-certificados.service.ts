import api from './api';

const ENDPOINT = '/usuarios-certificados';

export const usuariosCertificadosService = {
  // Aquí irán los métodos para interactuar con el endpoint de usuarios-certificados
};
