import api from './api';

const ENDPOINT = '/inscripcion-modalidades';

export const inscripcionModalidadesService = {
  // Aquí irán los métodos para interactuar con el endpoint de inscripcion-modalidades
};
